### node modules
```npm i```

### soft for generation new unique evm address
change ```walletsCount``` in ```main.ts``` to generate any number of wallets
all generated wallets are in ```wallets.csv```

command to run -> ```npm run dev```